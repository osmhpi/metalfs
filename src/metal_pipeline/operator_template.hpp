#pragma once

#include <memory>
#include <string>
#include <vector>
#include "abstract_operator.hpp"

namespace metal {

class OperatorTemplate {
public:
//    std::unique_ptr<AbstractOperator> build(std::vector<std::string> args);
};

} // namespace metal
